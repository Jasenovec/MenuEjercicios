<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema base reutilizable -->
    <style name="Base.Theme.AppDrawer" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:colorBackground">@android:color/white</item>
        <item name="android:textColor">@android:color/black</item>
        <!-- Configuraciones comunes adicionales -->
    </style>

    <!-- Tema específico que hereda del tema base -->
    <style name="AppTheme" parent="Base.Theme.AppDrawer">
        <item name="colorPrimary">@color/orange_500</item>
        <item name="colorPrimaryVariant">@color/orange_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/orange_200</item>
        <item name="colorSecondaryVariant">@color/orange_900</item>
        <item name="colorOnSecondary">@color/black</item>
    </style>
</resources>
